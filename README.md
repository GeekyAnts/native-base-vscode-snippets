# NativeBase Snippets for VS Code

This extension provides NativeBase snippets for React Native with NativeBase projects.

## Installation

In order to install an extension you need to launch the Command Pallete (Ctrl + Shift + P or Cmd + Shift + P) and type Extensions.
There you have either the option to show the already installed snippets or install new ones. Search for *NativeBase Snippets* and install it.

## Supported languages (file extensions)
* JavaScript (.js)
* JavaScript React (.jsx)

## Snippets

Below is a list of all available snippets and the triggers of each one. The **⇥** means the `TAB` key.

| Trigger  | Component |
| -------: | ------- |
| `a→`   | `Anatomy`: Container containing `Header`, `Content`, `Footer`|
| `ba→`   | `Badge`: Numerical indicator |
| `bu→`   | `Button` |
| `ca→`   | `Card`: Flexible, extensible container for `Content`|
| `cardd→`   | `Dynamic Card`: Card for vertically scrolling lists |
| `cardim→`   | `Card Image`: Card for image with icon below it |
| `cardi→`   | `Card Item`: Lists items in `Card` |
| `cards→`   | `Card Showcase`: Card for images with text and icon below it |
| `ch→`   | `Checkbox`: Select a number of items from a set of choices |
| `c→`   | `Col`: Column for Grid |
| `conta→`   | `Container`: Component to contain all other components |
| `cont→`   | `Content`: Body section of the screen |
| `dec→`   | `deckSwiper`: Swipable card |
| `f→`   | `FAB`: Floating Action Button |
| `g→`   | `Grid`: Grid layout |
| `h→`   | `H1`: Heading tag of font-size: 27 |
| `h2→`   | `H2`: Heading tag of font-size: 24|
| `h3→`   | `H3`: Heading tag of font-size: 21 |
| `he→`   | `Header`: Navbar of the screen |
| `ic→`   | `Icon` |
| `icont→`   | `Icon Textbox`: TextBox with Icon |
| `im→`   | `Image` |
| `l→`   | `List` |
| `lista→`   | `List Avatar`: Icon sized image with list item |
| `listd→`   | `Dynamic List`: List for vertically scrolling lists of changing data |
| `listi→`   | `List Icon`: Icon with list item |
| `listit→`   | `ListItem`: Define a list item |
| `listt→`   | `List Thumbnail`: Thumbnail sized image with list item |
| `p→`   | `Picker` |
| `ra→`   | `Radio Button` |
| `reg→`   | `Regular Textbox`: Rectangular Textbox |
| `rou→`   | `Rounded Textbox`: Textbox with rounded corner |
| `ro→`   | `Row`: Row in Grid |
| `se→`   | `Searchbar` |
| `sp→`   | `Spinner` |
| `t→`   | `Tabs`: Component for tabbed navigation|
| `te→`   | `Text` |
| `textb→`   | `Textbox` |
| `th→`   | `Thumbnail` |
| `u→`   | `Underlined Textbox`: Textbox with bottom border only |

[code]: https://code.visualstudio.com/
**Happy coding!**
